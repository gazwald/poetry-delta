#!/usr/bin/env bash
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

./scripts/build

echo "+++ Running poetry-delta +++"

docker run \
    --interactive \
    --rm \
    --tty \
    --volume "$(pwd):/mnt" \
    poetry-delta \
    "${@}"
